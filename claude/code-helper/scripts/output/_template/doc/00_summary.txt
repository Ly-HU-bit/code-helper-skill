================================================================================
CodeHelper Analysis Report — <PROJECT_NAME>
================================================================================
Date: <YYYY-MM-DD>
Target File(s): <list of modified files>
Preset: <preset_name | default>
Reference Style Guide: <URL or description>

================================================================================
SCORECARD — 代码质量评分
================================================================================

  Category       Score    Status    Detail
  ─────────────────────────────────────────────────────────────────
  Debug         <N>/10    <icon>    <N> bugs found (<N> high, <N> medium, <N> low)
  Style         <N>/10    <icon>    <N> violations (<N> naming, <N> spacing, ...)
  Testing       <N>/10    <icon>    <N> tests, <N> scenarios covered, <N> gaps
  Optimization  <N>/10    <icon>    <N>/<N> methods optimal
  ─────────────────────────────────────────────────────────────────
  OVERALL       <N.N>/10  <grade>   <A/B/C/D/F>

  Score Legend:
    10  = Perfect — no issues found
    8-9 = Good — minor issues only
    6-7 = Fair — some improvements needed
    4-5 = Poor — significant issues
    0-3 = Critical — major problems

  Grade Mapping: A(9-10) B(7-8) C(5-6) D(3-4) F(0-2)

==== Scoring Formula ===============================================

  NOTE: 直接填入最终代入后的公式与分值（如 10-(2×3+3×2+1×1)/2=2.5），
        不要写入中间推导、反复试算或犹豫过程。

  Debug:        10 - (high×3 + medium×2 + low×1) / 2  (min 0)
  Style:        10 - (high×2 + medium×1 + low×0.5)     (min 0)
  Testing:      10 if no gaps, 8 if ≤2 gaps, 6 if ≤5, 4 if ≤10, else 2
  Optimization: 10 if all optimal, 8 if 1 non-optimal, 6 if 2, else 4
  Overall:      (Debug + Style + Testing + Optimization) / 4

================================================================================
SUMMARY — 修改清单与统计
================================================================================

## 修改概览

| 类别 | 文件 | 详情 |
|------|------|------|
| Debug | 01_debug.txt | <N> 个问题（高: <N>, 中: <N>, 低: <N>） |
| Style | 02_style.txt | <N> 处违规（高: <N>, 中: <N>, 低: <N>） |
| Test  | 03_test.txt | 覆盖 <N> 个场景, 新增 <N> 个测试 |
| Opt   | 04_optimization.txt | <N> 个方法分析, <N> 处优化 |

## 所有修改文件

  - <file1> — <简要说明修改了什么>
  - <file2> — <简要说明修改了什么>
  ...

## 变更补丁

  已生成统一 diff 文件: changes.patch
  应用方式: git apply changes.patch
          或 patch -p1 < changes.patch

## 测试结果

  ✓ / ✗ <test summary after running src/run_tests.py>

## 阅读顺序建议

  1. 00_summary.txt（本文件） — 快速了解全貌 + 查看评分
  2. 01_debug.txt — 如有错误，优先阅读
  3. 02_style.txt — 代码风格改进
  4. 03_test.txt — 测试覆盖变化
  5. 04_optimization.txt — 性能分析
  6. changes.patch — 可直接应用的补丁文件

================================================================================
END OF SUMMARY
================================================================================
